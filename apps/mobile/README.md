# Mobile (Expo)

Bu klasör artık kök npm workspaces listesinde değil (VPS web build ile React çakışmasını önlemek için).

Yerel mobil geliştirme:

```bash
cd apps/mobile
npm install
npx expo start
```
